# City-Congestion-Tracker
City Congestion Tracker is an AI-powered traffic analytics system that stores synthetic congestion data in Supabase, exposes it through a FastAPI backend, visualizes trends in a dashboard, and generates plain-language congestion summaries using an LLM.
